============
Installation
============

At the command line::

    $ easy_install drf-phone-number-field

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv drf-phone-number-field
    $ pip install drf-phone-number-field
